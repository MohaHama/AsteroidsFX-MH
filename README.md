Komponentbaserede systemer, (F26) Mohammed Hamarash

1. Build the project from the root folder with the following Maven command: mvn clean install
2. Run the project from root folder with following Maven command: mvn exec:exec
